1) 26
2) Если в коллекции старых деффектов, ни один номер деффекта не равен ни одному номеру нового деффекта, страрый деффект становится не валидным. Замена метода FirstOrDefault() на метод First() приведет к исключению, если не будет найдено ни одного совпадения между номерами старых и новых деффектов.
3) 24
4) private unsafe double SimpleCopy(Coordinateltem* ccdltem) { return ccdltem->Odometer; }
5) 1 ошибка: value = value ?? currentValue (оператор проверки на null можно применять только к переменным одинакового типа, для исправления ошибки надо currentValue привести к ushort явным преобразованием). 2 ошибка binaryWriter.Write(value) (тк value nullable тип, в метод Write его надо передавать по значению binaryWriter.Write(value.Value)). В результате выполнения, метод запишет в файл в биарном виде нули, тк value всегда будет равно 0
6) Сперва на консоль будет выведено false, затем true
7) На первой строке консоли будет выведено O, на второй e\two